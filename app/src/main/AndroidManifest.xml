<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilproje">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- İnternet İzni -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- Rehber İzni -->
    <uses-permission android:name="android.permission.READ_CALENDAR" /> <!-- Takvim İzni -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobilProje">
        <activity
            android:name=".FiratPersonal"
            android:exported="false" />
        <activity
            android:name=".UmutMapsView"
            android:exported="false" />
        <activity
            android:name=".UmutProfileActivity"
            android:exported="false" />
        <activity
            android:name=".UmutPersonal"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBPRb8AuFPA2n7qI0VnP5IQl51lXx8GjNQ" />

        <activity
            android:name=".MelihMapsView"
            android:exported="false"
            android:label="@string/title_activity_melih_maps_view" />
        <activity
            android:name=".MelihPersonal"
            android:exported="false" />
        <activity
            android:name=".MelihProfileActivity"
            android:exported="false" />
        <activity
            android:name=".FiratProfileActivity"
            android:exported="false" />
        <activity
            android:name=".MainEntrance"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChoiceActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>